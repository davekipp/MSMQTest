<Window x:Class="MSMQTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MSMQTest"
        mc:Ignorable="d"
        Title="MSMQ Test Tool" Height="450" Width="800"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    
    <Grid Background="LightYellow">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.Column="0">
            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="0" Margin="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Label Content="Local IP" HorizontalContentAlignment="Right" VerticalContentAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="5"/>
                    <TextBox Name="txtLocalIPAddress" VerticalContentAlignment="Center" HorizontalAlignment="Stretch" Grid.Row="0" Grid.Column="1" Margin="5" TextChanged="OnTextChanged"/>

                    <Label Content="Local (In) Queue" VerticalContentAlignment="Center" HorizontalAlignment="Right" Grid.Row="1" Grid.Column="0" Margin="5"/>
                    <TextBox Name="txtLocalQueue" VerticalContentAlignment="Center" HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="1" Margin="5" TextChanged="OnTextChanged"/>

                    <Label Content="Remote IP" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Grid.Row="2" Grid.Column="0" Margin="5"/>
                    <TextBox Name="txtRemoteIPAddress" VerticalContentAlignment="Center" HorizontalAlignment="Stretch" Grid.Row="2" Grid.Column="1" Margin="5" TextChanged="OnTextChanged"/>

                    <Label Content="Remote (Out) Queue" VerticalContentAlignment="Center" HorizontalAlignment="Right" Grid.Row="3" Grid.Column="0" Margin="5"/>
                    <TextBox Name="txtRemoteQueue" VerticalContentAlignment="Center" HorizontalAlignment="Stretch" Grid.Row="3" Grid.Column="1" Margin="5" TextChanged="OnTextChanged"/>

                    <Label Content="Message Format" VerticalContentAlignment="Center" HorizontalAlignment="Right" Grid.Row="4" Grid.Column="0" Margin="5"/>
                    <RadioButton Content="XML" Name="rbXML" IsChecked="True" VerticalContentAlignment="Center" GroupName="Formatter" Grid.Row="4" Grid.Column="1" />
                    <RadioButton Content="Binary" Name="rbBinary" VerticalContentAlignment="Center" GroupName="Formatter" Grid.Row="5" Grid.Column="1" />

                    <Button Content="Connect" Name="btnConnect" Click="OnConnectBtnClick"  IsEnabled="False" VerticalContentAlignment="Center" Grid.Row="6" Grid.Column="1" Margin="6"/>
                </Grid>
            </Border>
        </StackPanel>

        <GridSplitter 
            Grid.Column="1" 
            Width="5" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Stretch"
            ShowsPreview="True"
            />

        <Border Grid.Column="2" BorderBrush="Black" BorderThickness="1" Margin="5">
            <DockPanel >
                <DockPanel DockPanel.Dock="Bottom" Margin="5">
                    <Button Content="Send" Name="btnSend" Click="OnSendBtnClick" IsDefault="True" IsEnabled="False" DockPanel.Dock="Right" Margin="5"/>
                    <TextBox Name="txtMessage" TextChanged="OnTextChanged" HorizontalAlignment="Stretch" Margin="5"/>
                </DockPanel>
                <Border BorderBrush="Black" BorderThickness="1" Margin="5">
                    <TextBox Text="" Name="txtLog" TextWrapping="WrapWithOverflow" Background="White" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                </Border>
            </DockPanel>
        </Border>
        
    </Grid>
</Window>
